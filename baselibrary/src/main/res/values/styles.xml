<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <declare-styleable name="TagCloudView">

        <!-- 背景 -->
        <attr name="tagBackground" format="reference" />
        <!-- 文字颜色 -->
        <attr name="tagTextColor" format="color" />
        <!-- 文字大小 -->
        <attr name="tagTextSize" format="dimension" />
        <!-- -->
        <attr name="tagVerticalMargen" format="dimension" />
        <!-- -->
        <attr name="tagHorizontalMargen" format="dimension" />
        <!-- 是否可点击 -->
        <attr name="tagCanClick" format="boolean" />

    </declare-styleable>

    <style name="title_text_style">
        <item name="android:textColor">@color/White</item>
        <item name="android:textSize">@dimen/dp750_40</item>
    </style>


</resources>
